import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "gs-expertise-two.vercel.app",
      "www.erawati.com",
      "imag.malavida.com",
      "www.tendancehotellerie.fr",
    ],
  },
  /* config options here */
};

export default nextConfig;
